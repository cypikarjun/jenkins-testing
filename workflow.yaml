apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: pipekit-demo-
  namespace: argo
spec:
  serviceAccountName: argo-argo-workflows-workflow-controller
  entrypoint: whalesay
  templates:
  - name: whalesay
    container:
      image: docker/whalesay
      command: [cowsay]
      args: ["hello from pipekit"]
